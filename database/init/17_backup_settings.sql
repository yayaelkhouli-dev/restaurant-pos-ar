-- إعدادات النسخ الاحتياطي (Backup settings)
--
-- تُدرج القيم الافتراضية مرة واحدة فقط. النسخ التلقائي مُطفأ افتراضياً حتى
-- يفتحه مدير النظام من شاشة «النسخ الاحتياطي» ويختار مكاناً خارجياً للنسخة
-- الثانية (فلاشة أو هارد خارجي أو مجلد على الشبكة).
--
-- ملاحظة مهمة: في معظم الأجهزة تكون كل الأقسام (C: و D: ...) على نفس القرص
-- الفيزيائي الواحد، فالنسخة المحفوظة بجوار قاعدة البيانات تموت معها إذا تلف
-- القرص. المكان الخارجي هو النسخة الوحيدة التي تنجو.

INSERT INTO settings (key, value) VALUES
    ('backup_auto_enabled',   'false'),
    ('backup_interval_hours', '6'),
    ('backup_retention_days', '30'),
    ('backup_external_path',  ''),
    ('backup_last_at',        ''),
    ('backup_last_status',    '')
ON CONFLICT (key) DO NOTHING;
